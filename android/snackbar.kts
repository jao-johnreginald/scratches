//val snackBar = Snackbar.make(binding.root, "Records cleared successfully!", Snackbar.LENGTH_SHORT)
//snackBar.anchorView = binding.bottomNav
//snackBar.setAction("Undo") { //some code to restore the records }
//snackBar.show()

// SnackBar, first parameter gets the context of the view passed in, doesn't matter what view
// anchorView places the snackBar above another view
// setAction to add a button and pass the code in the {  }